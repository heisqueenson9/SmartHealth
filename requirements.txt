# Smart Health Sync — Python Dependencies
# Authors: Enock Queenson Eduafo & Christabel Araba Edumadze | University of Ghana 2026

# ── Core Framework ────────────────────────────────────────────
flask>=3.0.3,<4.0
flask-cors>=4.0.0,<5.0
flask-sqlalchemy>=3.1.1,<4.0
flask-migrate>=4.0.7,<5.0
flask-limiter>=3.7.0,<4.0
Werkzeug>=3.0.3,<4.0

# ── Database ──────────────────────────────────────────────────
psycopg2-binary>=2.9.9,<3.0

# ── ML / Data Science ────────────────────────────────────────
# IMPORTANT: These MUST match the versions used to train the .pkl model files.
# Models were trained with sklearn 1.8.0 / numpy 2.4.x / joblib 1.5.x.
# Using older versions will cause "ModuleNotFoundError" or unpickling crashes.
numpy==2.4.3
scikit-learn==1.8.0
joblib==1.5.3
pandas==2.3.3
scipy==1.17.1

# ── AI Explainer ──────────────────────────────────────────────
groq>=0.9.0
requests>=2.31.0

# ── Mail (optional — skipped gracefully if not installed) ─────
resend>=2.0.0

# ── Production Server ─────────────────────────────────────────
gunicorn>=22.0.0

# ── Testing ───────────────────────────────────────────────────
pytest>=8.2.0
pytest-flask>=1.3.0
